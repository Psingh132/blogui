import { Category } from "../../category/models/category.model";

export interface BlogPostHome {
    title: string;
    shortDescription: string;
    featuredImageUrl: string;
    urlHandle: string;
}